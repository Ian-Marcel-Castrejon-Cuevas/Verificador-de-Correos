# Verificador-de-Correos
Es una aplicación en Java con interfaz gráfica (Swing) que permite validar direcciones de correo electrónico usando expresiones regulares. Además analiza el correo en tokens (usuario, arroba, dominio y extensión) y verifica si el dominio y la extensión pertenecen a listas permitidas.
